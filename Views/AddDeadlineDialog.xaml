<Window x:Class="DueTick.Views.AddDeadlineDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Deadline"
        Icon="pack://application:,,,/DueTick;component/Resources/icon.png"
        Height="450"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">
    <Border Background="{StaticResource WindowBackgroundBrush}"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="#D0D0D0"
            Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Add Deadline" FontSize="14" FontWeight="SemiBold"
                           Margin="16,12,16,8" Foreground="{StaticResource AccentBrush}"/>
                <Button Content="✕" Width="40" Height="40" Grid.Column="1"
                        Margin="0,8,12,8" Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource SecondaryTextBrush}"
                        FontSize="14" Click="CloseButton_Click"/>
            </Grid>

            <Grid Grid.Row="1" Margin="24,0,24,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="Title" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <TextBox x:Name="TitleTextBox" Margin="0,0,0,20" Height="32" VerticalContentAlignment="Center"/>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Date" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <DatePicker x:Name="DueDatePicker" Height="32"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Time" FontWeight="SemiBold" Margin="0,0,0,8"/>
                            <TextBox x:Name="DueTimeTextBox" Text="12:00" Height="32" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Reminder" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <ComboBox x:Name="ReminderComboBox" Height="32" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Cancel" Width="100" Height="32" Margin="0,0,12,0" Click="CancelButton_Click"/>
                    <Button Content="Add" Style="{StaticResource AccentButtonStyle}" Width="100" Height="32" IsDefault="True" Click="AddButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
