<Window x:Class="DueTick.Views.WidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DueTick Deadlines"
        Icon="pack://application:,,,/DueTick;component/Resources/icon.png"
        Height="300"
        Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        MouseDown="Window_MouseDown">
    <Border Background="{StaticResource WindowBackgroundBrush}"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="#D0D0D0"
            Padding="0"
            Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Upcoming Deadlines" FontSize="14" FontWeight="SemiBold"
                           Margin="16,12,16,8" Foreground="{StaticResource AccentBrush}"/>
                <Button Content="✕" Width="40" Height="40" Grid.Column="1"
                        Margin="0,8,12,8" Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource SecondaryTextBrush}"
                        FontSize="14" Click="CloseButton_Click"/>
            </Grid>

            <ListBox x:Name="DeadlinesListBox" Grid.Row="1" Margin="8,0,8,12"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="ItemBorder"
                                            Background="{StaticResource CardBackgroundBrush}"
                                            CornerRadius="4"
                                            BorderThickness="1"
                                            BorderBrush="#E5E5E5"
                                            Margin="4,4,4,4"
                                            Padding="12,10">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#F9F9F9"/>
                                            <Setter TargetName="ItemBorder" Property="BorderBrush" Value="#D0D0D0"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14"/>
                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                    <TextBlock Text="{Binding DueDate, StringFormat='{}{0:g}'}"
                                               FontSize="12" Foreground="{StaticResource SecondaryTextBrush}"/>
                                    <TextBlock Text="{Binding ReminderOffset, StringFormat=' (remind {0:hh\\:mm} before)'}"
                                               FontSize="12" Foreground="#8A8A8A"/>
                                </StackPanel>
                            </StackPanel>
                            <Button Content="Delete" Width="60" Height="28" Grid.Column="1"
                                    VerticalAlignment="Center" Margin="10,0,0,0"
                                    Click="DeleteButton_Click" FontSize="12"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
