<Window x:Class="DueTick.Views.SettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Icon="pack://application:,,,/DueTick;component/Resources/icon.png"
        Height="360"
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">
    <Border Background="{StaticResource WindowBackgroundBrush}"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="#D0D0D0"
            Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Settings" FontSize="14" FontWeight="SemiBold"
                           Margin="16,12,16,8" Foreground="{StaticResource AccentBrush}"/>
                <Button Content="✕" Width="40" Height="40" Grid.Column="1"
                        Margin="0,8,12,8" Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource SecondaryTextBrush}"
                        FontSize="14" Click="CloseButton_Click"/>
            </Grid>

            <Grid Grid.Row="1" Margin="24,0,24,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <CheckBox x:Name="AutoStartCheckBox" Content="Auto-start with Windows" Margin="0,0,0,16"/>
                    <CheckBox x:Name="ClipboardCheckBox" Content="Monitor clipboard for deadlines" Margin="0,0,0,16"/>
                    <CheckBox x:Name="WidgetCheckBox" Content="Show widget window" Margin="0,0,0,16"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button Content="Cancel" Width="100" Height="32" Margin="0,0,12,0" Click="CancelButton_Click"/>
                    <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Width="100" Height="32" IsDefault="True" Click="SaveButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
